// Code generated by @sumup/sumup-ts-codegen. DO NOT EDIT.
import type { HTTPClient } from "./client";
import { EventBase, type EventPayload, UnknownEvent } from "./event";
import type { Member } from "./types/member";
import type { Reader } from "./types/reader";
/** A members.created notification. Call fetchObject() to retrieve the latest Member. */
export class MemberCreatedEvent extends EventBase<Member> {
  declare readonly type: "members.created";
}
/** A members.deleted notification. Call fetchObject() to retrieve the latest Member. */
export class MemberDeletedEvent extends EventBase<Member> {
  declare readonly type: "members.deleted";
}
/** A members.updated notification. Call fetchObject() to retrieve the latest Member. */
export class MemberUpdatedEvent extends EventBase<Member> {
  declare readonly type: "members.updated";
}
/** A readers.created notification. Call fetchObject() to retrieve the latest Reader. */
export class ReaderCreatedEvent extends EventBase<Reader> {
  declare readonly type: "readers.created";
}
/** A readers.deleted notification. Call fetchObject() to retrieve the latest Reader. */
export class ReaderDeletedEvent extends EventBase<Reader> {
  declare readonly type: "readers.deleted";
}
/** Known wire event names and their notification classes. */
export interface EventMap {
  "members.created": MemberCreatedEvent;
  "members.deleted": MemberDeletedEvent;
  "members.updated": MemberUpdatedEvent;
  "readers.created": ReaderCreatedEvent;
  "readers.deleted": ReaderDeletedEvent;
}
/** A recognized event notification or {@link UnknownEvent}. Narrow with instanceof to access a specific resource type. */
export type EventNotification = EventMap[keyof EventMap] | UnknownEvent;
/** @internal Expected object types from the event specification. */
export const eventObjectTypes: Record<string, string> = {
  "members.created": "member",
  "members.deleted": "member",
  "members.updated": "member",
  "readers.created": "reader",
  "readers.deleted": "reader",
};
/** @internal Construct the typed notification after envelope validation. */
export function createEvent(
  payload: EventPayload,
  client: HTTPClient,
): EventNotification {
  switch (payload.type) {
    case "members.created":
      return new MemberCreatedEvent(payload, client);
    case "members.deleted":
      return new MemberDeletedEvent(payload, client);
    case "members.updated":
      return new MemberUpdatedEvent(payload, client);
    case "readers.created":
      return new ReaderCreatedEvent(payload, client);
    case "readers.deleted":
      return new ReaderDeletedEvent(payload, client);
    default:
      return new UnknownEvent(payload, client);
  }
}
